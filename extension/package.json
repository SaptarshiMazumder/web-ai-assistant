{
  "name": "web-ai",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild src/popup.ts --bundle --outfile=dist/popup.js && esbuild src/content.ts --bundle --outfile=dist/content.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/chrome": "^0.1.1",
    "esbuild": "^0.25.8",
    "typescript": "^5.8.3"
  }
}